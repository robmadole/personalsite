Open the main documentation at:

    http://localhost:5001

Run the development server:

    $ develop

Run the Python application tests:

    $ nosetests

Deploy the site to robmadole.com:

    $ deploy

To add a location to the bookmarks:

    $ addloc URL CATEGORYSLUG[, CATEGORYSLUG...]

